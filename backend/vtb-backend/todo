//Use controllers collector as in docs

//Set up sequelize env to work with the local database (use config.js instead of the config.json file)
Readme.md with the project setup process/description
Remove redundant token checks in endpoints
Task name must be unique only for user, not for the whole DB
Move password hashing into the User model (setters)
Set different route names for single item request and for multiple item reequest
User should be able to work with his own messages
Move validation errors processing outside controllers (DRY)

Forntend:
Add logged user name
Use destruction instead (props);

role separation:

1.  in projects - admin gets all projects
2.  buttons
3.  hide left menu button

+search
+result filter
+pending
+pagination
+modals
+refetch
